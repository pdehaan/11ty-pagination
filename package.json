{
  "name": "11ty-pagination",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "eleventy build --quiet",
    "prebuild": "rimraf _site",
    "format:dist": "npx prettier _site/**/*.html --write --loglevel=warn",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Peter deHaan",
  "license": "MPL-2.0",
  "devDependencies": {
    "@11ty/eleventy": "^0.10.0",
    "axios": "^0.19.2",
    "rimraf": "^3.0.0",
    "slugify": "^1.3.6"
  }
}
